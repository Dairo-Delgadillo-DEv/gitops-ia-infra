apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: telegram-config
  namespace: monitoring
  labels:
    release: grafana-stack
spec:
  alertmanagerConfigNamespace: {}
  route:
    groupBy: ['alertname']
    groupWait: 10s
    groupInterval: 10s
    repeatInterval: 5m
    receiver: 'telegram-receiver'
    matchers:
    - name: alertname
      value: ApiServerHighTraffic
  receivers:
  - name: 'telegram-receiver'
    telegramConfigs:
    - botToken:
        name: telegram-alerts-secret
        key: token
      chatID: 1943944263
      sendResolved: true
      parseMode: 'HTML'S
